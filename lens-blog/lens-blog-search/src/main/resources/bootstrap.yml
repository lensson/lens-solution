server:
  port: 9013
spring:
  application:
    name: lens-blog-search
  cloud:
    nacos:
      discovery:
        server-addr: ${lens-nacos:localhost}:8848
        metadata:
          VERSION: 1.0
      config:
        server-addr: ${lens-nacos:localhost}:8848
        file-extension: yaml