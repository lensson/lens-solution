server:
  port: 9014
spring:
  application:
    name: lens-blog-search
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos:localhost}:8848
        metadata:
          VERSION: 1.0
      config:
        server-addr: ${nacos:localhost}:8848
        file-extension: yaml