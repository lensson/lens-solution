server:
  port: 9020
spring:
  application:
    name: lens-blog-monitor
  cloud:
    nacos:
      discovery:
        server-addr: ${lens-nacos:localhost}:8848
        metadata:
          VERSION: 1.0
      config:
        server-addr: ${lens-nacos:localhost}:8848
        file-extension: yaml