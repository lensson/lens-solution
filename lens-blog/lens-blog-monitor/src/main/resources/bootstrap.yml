server:
  port: 9013
spring:
  application:
    name: lens-blog-monitor
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos:localhost}:8848
        metadata:
          VERSION: 1.0
      config:
        server-addr: ${nacos:localhost}:8848
        file-extension: yaml