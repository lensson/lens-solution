server:
  port: 8850
spring:
  application:
    name: lens-auth
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos:localhost}:8848
        metadata:
          VERSION: 1.0
      config:
        server-addr: ${nacos:localhost}:8848
        file-extension: yaml